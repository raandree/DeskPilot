---
####################################################
#          ModuleBuilder Configuration             #
####################################################
BuiltModuleSubdirectory: module
# web/ (relocated under source/) is bundled verbatim into the built module so a
# Gallery-installed Start-DeskPilot serves the SPA from the module folder.
# browser/ ships the same way: it holds the supervisor sources and the Playwright
# pin, which are copied into the data directory at install time because the
# module folder may be read-only and is replaced wholesale by an update.
CopyPaths:
  - web
  - browser
Encoding: UTF8
prefix: Prefix.ps1
VersionedOutputDirectory: true

####################################################
#       Sampler Pipeline Configuration             #
####################################################
BuildWorkflow:
  '.':
    - build
    - test

  build:
    - Clean
    - Build_Module_ModuleBuilder
    - Build_NestedModules_ModuleBuilder
    - Create_changelog_release_output

  pack:
    - build
    - package_module_nupkg

  # Windows CurrentUser package: the built module plus its install/uninstall
  # scripts, a hashed file inventory and an SBOM. Deliberately not part of
  # 'build' or 'pack' so normal module build semantics are unchanged.
  packwin:
    - build
    - pack_windows

  test:
    - Pester_Tests_Stop_On_Fail
    - Convert_Pester_Coverage
    - Pester_if_Code_Coverage_Under_Threshold

  publish:
    - Publish_Release_To_GitHub
    - publish_module_to_gallery

####################################################
#       PESTER  Configuration                      #
####################################################
Pester:
  OutputFormat: NUnitXML
  ExcludeFromCodeCoverage:
  Script:
    - tests/QA/module.tests.ps1
    - tests/Unit
  ExcludeTag:
  Tag:
  CodeCoverageThreshold: 0

ModuleBuildTasks:
  Sampler:
    - '*.build.Sampler.ib.tasks'
  Sampler.GitHubTasks:
    - '*.ib.tasks'

TaskHeader: |
  param($Path)
  ""
  "=" * 79
  Write-Build Cyan "`t`t`t$($Task.Name.replace("_"," ").ToUpper())"
  Write-Build DarkGray  "$(Get-BuildSynopsis $Task)"
  "-" * 79
  Write-Build DarkGray "  $Path"
  Write-Build DarkGray "  $($Task.InvocationInfo.ScriptName):$($Task.InvocationInfo.ScriptLineNumber)"
  ""

####################################################
#                GitHub Configuration              #
####################################################
GitHubConfig:
  GitHubFilesToAdd:
    - 'CHANGELOG.md'
  GitHubConfigUserName: Raimund
  GitHubConfigUserEmail: r.andree@live.com
