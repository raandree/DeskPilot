# Progress

## Status

**Phase:** Bootstrap shipped — specs, Memory Bank, Host Server, web UI, launcher,
and tests are in place and verified.

## Shipped

| Date | Change |
| --- | --- |
| 2026-07-09 | **Branch-merge audit (read-only, no code changed).** Answered "which feature branches hold unmerged work?" by verifying live git rather than the drifted log. Only three local branches exist: `main` (in sync with `origin/main` at tag `v0.2.0-preview0002` — so `main` IS pushed, correcting the "local-only" notes), `ai/fix-clean-machine-auth`, and `ai/atelier-setup` (HEAD). **`ai/atelier-setup` is the only branch with unmerged work** — 3 commits / 17 files / +668 lines (CopilotAtelier setup + agent auto-refresh + Refresh-button removal); clean fast-forward into `main`. **`ai/fix-clean-machine-auth` has no unmerged work**: `git cherry` marks its commit `-` and a direct tip-to-tip diff of `source/`+`tests/`+`specs/`+`CHANGELOG.md` vs `main` is empty (the fix reached `main` under a different SHA, not via the recorded merge commit `734e646`, now a dangling object). All older `ai/*` branches no longer exist locally — their work is already in `main`. |
| 2026-07-09 | **Removed the redundant "Refresh agents" button from the CopilotAtelier setup modal, on `ai/atelier-setup` (local-only).** Now that the Agent list auto-refreshes (interval + window focus/visibility), the manual button in the setup result modal is unnecessary; removed it (leaving only **Close**) and reworded the success copy to say new agents appear in the Agent menu automatically. This also removes an `atelier-refresh` id collision the button shared with the Settings Atelier-health control. Frontend-only (`app.js`); no `.ps1` change, so the built/test surface is unchanged. Verified: `app.js` ESM check OK; language-service clean. |
| 2026-07-09 | **Made the Agent list refresh without a restart, on `ai/atelier-setup` (local-only).** Follow-up to the CopilotAtelier setup: the user reported the Agent menu still needed a DeskPilot restart to show newly-installed agents and asked for auto-refresh. Root cause: `agentsRoot` resolved once at startup via `Get-DpCopilotDefaults` returns `$null` when `~/.copilot/agents` is absent (the fresh-machine case), so after CopilotAtelier created the junction the running session kept a null root and `GET /api/agents` stayed empty until a restart. Fix: new `Resolve-DpAgentsRoot` helper (configured root as-is, else the conventional `~/.copilot/agents` only when it now exists — no phantom path); the `agents` route adopts + persists it live so the junction is picked up and a selected Agent reaches Turn assembly. Plus frontend `wireAgentsAutoRefresh` — polls `loadAgents` every 15s while visible + on window focus/visibilitychange (catches returning from the setup console), skipping while streaming or the menu is open; `loadAgents` keeps the previous list on transient error. Polling chosen over a server-side FileSystemWatcher (single-threaded Host Server, no persistent SSE). +3 unit tests. Docs: CHANGELOG, specs 030/040. Verified: full Sampler build+test **367/367, 0 failed** (16 tasks, 0 errors, 0 warnings); `app.js` ESM check OK. |
| 2026-07-09 | **Added an opt-in CopilotAtelier setup to the Agent menu, on `ai/atelier-setup` (local-only, not merged/pushed).** The user asked to set up [CopilotAtelier](https://github.com/raandree/CopilotAtelier) from the Agent picker; since it is a repository (not an installable module), DeskPilot now downloads it and runs its `Setup-CopilotSettings.ps1` after explicit consent (the user's requirement: not one-click). `buildAgentMenu` always shows a **Set up CopilotAtelier…** action that opens a consent modal (`#atelier-modal`) spelling out what the script changes (`~/.copilot` junctions, VS Code `settings.json`/`keybindings.json`, the `COPILOT_ALLOW_ALL` env var) before anything runs; on confirm it POSTs `/api/atelier/setup`. Two new Private helpers: `Get-DpAtelierSource` (downloads the `main` zipball over HTTPS from the fixed first-party URL, extracts, normalises to the canonical `CopilotAtelier` folder under `<dataDir>/atelier`; never throws) and `Invoke-DpAtelierSetup` (locates the setup script and, on Windows, launches it in a visible `pwsh` console the user drives so its own `Read-Host` safety prompts work — DeskPilot never answers them; non-Windows returns the path for a manual run; `-Launcher`/`-IsWindowsPlatform` injectable for tests). New `atelierSetup` route + handler; frontend `openAtelierSetup`/`renderAtelierConsent`/`runAtelierSetup` + a Refresh-agents step. Security: fixed first-party HTTPS URL (no user URL → no SSRF), consent gate, user-driven console, Windows-scoped auto-run (specs/050 T12). +4 unit tests (`Invoke-DpAtelierSetup`). Docs: CHANGELOG, specs 010 (FR-X7)/040/050, glossary. Verified: full Sampler build+test **364/364, 0 failed** (16 tasks, 0 errors, 0 warnings); both helpers PSSA-clean; `app.js` ESM check OK. |
| 2026-07-09 | **Merged the clean-machine sign-in fix into `main` (local-only, not pushed).** Merged `ai/fix-clean-machine-auth` into `main` via merge commit `734e646` (parents `458e250` the fix + `b1550be` the prior main tip). The merge was **clean — no conflicts**: the fix branch was based on the gallery tip `ced3fc6` that `main` already contained (gallery was merged earlier via PR #1 / `b1550be`, tagged `v0.2.0-preview0001`), so the auth-fix changes applied on top untouched. `main` is now 2 commits ahead of `origin/main` (the fix + the merge commit), unpushed. Verified green immediately after the merge: full Unit suite **207/207, 0 failed**. `ai/fix-clean-machine-auth` kept (not deleted). |
| 2026-07-09 | **Fixed clean-machine sign-in, broken by the Engine's token-file rename.** A user reported that authenticating in DeskPilot never completed on a fresh machine (they had installed ShellPilot `0.2.1-preview0001` first). Root cause: ShellPilot renamed its cached OAuth token from `.copilot-demo-token` to `.shellpilot-token` (0.2.1); `Initialize-DpEngine` hardcoded the old name, so on a clean machine the device-code flow completed and the Engine wrote `~/.shellpilot-token`, but DeskPilot's post-auth `Test-Path Engine.TokenPath` (plus `/api/health`, `/api/auth/status`) checked the old name -> `authenticated:false` -> the SPA looped on "Sign-in did not complete. Try again." Only clean machines hit it (a stale token short-circuits `Invoke-DpAuthFlow` as already-signed-in), which is why it was never seen on a dev machine. Fix (one point, `Initialize-DpEngine`): after import, probe the Engine's own `$script:DefaultTokenPath` via `& (Get-Module ShellPilot) { $script:DefaultTokenPath }` and use it for `Engine.TokenPath`, falling back to `.shellpilot-token` (preferring an existing legacy `.copilot-demo-token`) only if the probe is unavailable — version-agnostic to the Engine's token filename. Verified: probe reproduced live against ShellPilot 0.2.0 on disk (returns its `.copilot-demo-token` default, i.e. reads whatever the imported Engine uses); `Initialize-DpEngine.ps1` AST 0 errors; PSSA clean; full Unit suite 207/207; classifier describes 19/19. Docs: CHANGELOG (Fixed), techContext auth fact, specs/050 T4; two classifier test sample strings refreshed. Note: the attached video could not be decoded, so the diagnosis was reconstructed from the auth code path + the current ShellPilot source. On `ai/fix-clean-machine-auth` (local-only). |
| 2026-07-08 | **Made DeskPilot publishable from the PowerShell Gallery with the web UI bundled, on `ai/gallery-web-bundle` (local-only; not merged/pushed/published).** After a fresh signed-off grill-me interview, relocated `web/` -> `source/web/` (git mv) and set `build.yaml` `CopyPaths: ['web']` so ModuleBuilder bundles the SPA into the built module; dropped the public `-WebRoot` from `Start-DeskPilot` (now resolves `$PSScriptRoot/web` with a `DESKPILOT_WEB_ROOT` env override used by the dev launcher + tests for hot-reload) and added a fail-fast bundle check before binding; derived the version from the manifest (removed hardcoded `0.1.0`). Fixed manifest metadata for the Gallery (Author `Raimund Andree`, ProjectUri raandree/DeskPilot, IconUri, Description/ReleaseNotes/Tags) and kept ShellPilot resolved at runtime by `Resolve-DpEngineModule` (NOT a hard manifest dependency, which would break import when absent; build `RequiredModules.psd1` tracks `ShellPilot = 'latest'`). Added MIT `LICENSE`, README Gallery/CI badges + a Gallery-install quick-start, and a fail-silent, non-blocking launch-time update check (`Get-DpUpdateNotice`, +5 tests, surfaced via a background job on an idle accept-loop iteration). New `tests/Unit/WebAssets.Tests.ps1` (bundle presence, Linux case-sensitivity, public-Gallery secret scan). Caught + fixed a Pester trap: the first WebAssets version used `-ForEach`+`BeforeDiscovery`, failed discovery (2 NotRun) and did NOT fail the build; rewrote both guards as run-time loops inside one `It`. Docs: CHANGELOG, techContext, specs 020/040. Verified: full Sampler build+test green twice - 17 tasks, 0 errors, 0 warnings; built `0.2.0-ai`; WebAssets 4/4, Start-DeskPilot 3/3, 0 failed / 0 NotRun. Scope was publishable-but-unpublished: no publish, no publish secrets set (`GalleryApiToken`/`GitHubToken`; there is no `PUBLISH_ENABLED` variable in the current ShellPilot-mirroring ci.yml), no push/merge. |
| 2026-07-08 | **Merged `ai/stream-smoothness` into `main` (local-only, not pushed).** Fast-forwarded `aa95a5d..1e032ac` — the single perf commit `1e032ac` (items 3 + 4: SSE token-frame coalescing + explorer-poll pause during a Turn). Verified green immediately before the merge: full Sampler build+test **348/348, 0 failed** (16 tasks, 0 errors, 0 warnings). A first re-verify attempt had failed on a transient file lock on the built `output/module/DeskPilot/.../DeskPilot.psm1` (a lingering session held the imported module); cleared by deleting `output/module/DeskPilot` and rebuilding green — environmental, not a code issue. Working tree clean; `ai/stream-smoothness` kept (not deleted); `main` is ahead of `origin/main` by 1, unpushed. |
| 2026-07-08 | **Improved live-streaming smoothness (perf items 3 + 4), on `ai/stream-smoothness` (local-only).** After analysing DeskPilot + ShellPilot vs the GHCP VS Code extension, implemented the two DeskPilot-only fixes. **Item 3:** `Invoke-DpTurn`'s Engine Information-stream drain `Start-Sleep` dropped 40 ms -> 10 ms, and the emit path now coalesces consecutive same-kind text records (`delta`/`reasoning`) into one buffered SSE frame per drain (new `$flush` + `pendingEvent`/`pendingText` on `$turnState`) rather than one `ConvertTo-Json` + auto-flushed socket write per token — cutting per-token CPU/socket overhead and the chunky 40 ms cadence; a `tasks` frame or a kind change flushes first (exact ordering), and `emitted` counts buffered records so the transient-error retry still never duplicates output. Heartbeat cadence unchanged in wall-clock (~10 s, 1000 x 10 ms). **Item 4:** the file explorer's 5 s auto-refresh `tick` now returns early while `state.streaming`, so the single-threaded Host Server no longer services a directory + Git scan inline in the streaming loop (mid-turn stall); the turn's `finally` still refreshes once on end. Items 1 + 2 (ShellPilot session-token cache + pooled `HttpClient`) handed off as a copy-paste prompt (Engine sacrosanct). No new tests (the coalescing lives in `Invoke-DpTurn`'s runspace loop, not unit-testable in isolation; the `Get-DpStreamFrame` classifier is unchanged and its 10 tests still pass). Verified: full Sampler build+test **348/348, 0 failed** (16 tasks, 0 errors, 0 warnings); AST parse 0 errors; PSSA one pre-existing BOM/ellipsis warning (line ~101, not my code); `app.js` ESM check exit 0. |
| 2026-07-08 | **Merged `ai/settings-tabs` into `main` (local-only, not pushed).** Fast-forwarded `96a650c..f437b45` — the Settings-drawer tabs (`a5b3e22`) plus the streamed-Thinking line-break fix (`f437b45`, which absorbed the two `source/`/`tests/` files left uncommitted last turn). Verified green immediately before the merge: full Sampler build+test **348/348, 0 failed** (16 tasks, 0 errors, 0 warnings). Working tree clean; `ai/settings-tabs` kept (not deleted); `main` is now ahead of `origin/main` by 6 commits, unpushed. |
| 2026-07-08 | **Fixed the model's Thinking losing its line breaks in the browser.** The user reported streamed *Thinking* running together with no line breaks (e.g. "…before I consolidate.GitHub code search needs auth."). Root cause was DeskPilot-side: the Engine emits each reasoning/trace line as one `Write-Host` record whose `HostInformationMessage.Message` omits the trailing newline (the break lives in the separate `NoNewLine` flag — `$false` for a complete line, `$true` for a `-NoNewline` streamed token); `Get-DpStreamFrame` streamed each record verbatim and the SPA concatenates the `reasoning`/`delta` frames, so complete lines glued into one run-on wall. Fix (single point, `Get-DpStreamFrame`): capture `$completeLine = ($messageData.NoNewLine -eq $false)` and re-attach `` `n `` to the emitted text for complete-line writes only — streamed tokens (`$true`) and unspecified (`$null`) are untouched, so the answer token stream is unchanged and existing tests still pass. SSE-safe: object payloads are JSON-encoded so the newline becomes an escaped `\n` that survives `ConvertTo-DpSseFrame`'s flatten and is restored by the client `JSON.parse` (the same mechanism the final answer Markdown relies on). No frontend change (Thinking pane is already `white-space: pre-wrap`). +4 unit tests. CHANGELOG `[Unreleased] → Fixed`. Verified: `Get-DpStreamFrame` describe **10/10**; full Unit helpers file **336/336, 0 failed**; AST parse 0 errors; PSScriptAnalyzer clean on the changed helper. On `ai/persistent-memory` (local-only). |
| 2026-07-08 | **Grouped the Settings drawer into tabs.** The drawer had grown to ~23 stacked fields behind one scrollbar; reorganised them into six tabs — **General** (model, reasoning effort, show thinking, task tracking, max iterations, theme), **Permissions**, **Projects** (projects + reference files), **Customizations** (Skill/Instruction/Prompt roots, Agents folder, Atelier health), **Memory & context** (User profile, Agent memory, learn toggle, auto-compaction), **Engine & data** (spend warning, Engine status, back up & restore) — shown as a sticky, wrapping pill strip (`.settings-tabs`) at the top of the drawer. **Every panel stays in the DOM (only the active one shown)** so all field handlers keep binding by id unchanged; new `wireSettingsTabs(body)` toggles panels and implements the WAI-ARIA tablist keyboard pattern (Left/Right/Home/End) with roving `tabIndex`. Frontend-only: `openSettings` template + `wireSettingsTabs` in `app.js`; `.settings-tabs`/`.settings-tab-btn`/`.settings-tab` in `styles.css`; specs/040 §6 + CHANGELOG. Verified: `app.js` ESM check OK (exit 0); app.js + styles.css report 0 errors. On `ai/settings-tabs` (local-only). |
| 2026-07-08 | **Merged the memory work into `main` (local-only, not pushed).** Fast-forwarded `862bce2..a748b00` — bringing the Hermes-inspired auto-compaction + Usage batch (`7ca8f69`), the `Get-DpAgentList` inaccessible-root fix (`738ab66`), and persistent memory / User Profile + Agent Memory (`a748b00`). Working tree clean; full Sampler build+test was **344/344** green at the tip and `app.js` ESM check OK. `ai/memory-context-batch` and `ai/persistent-memory` kept (not deleted). |
| 2026-07-07 | **Shipped persistent memory (User Profile + Agent Memory) on `ai/persistent-memory` (local-only, not merged).** The user approved building the Hermes-style memory "a bit bigger, my call on size." Two bounded stores injected into every Turn's system prompt (fenced as reference-not-instructions): the **User Profile** (the manual `preferences` block, reframed; 8,000 chars) and a new agent-curated **Agent Memory** (12,000 chars, `agent-memory.json`) — combined ~5,000 tokens worst case, ~5× Hermes's ~3,600, still <5% of a 128k window. Curated **autonomously** (`maybeLearnMemory` in `_runTurn`'s finally, throttled to every 5th assistant Turn, best-effort, re-entrancy-guarded, toasts only on change; `memoryLearning` default on) and **manually** (an "Update from this conversation" button + view/edit/clear in Settings → Memory), each via a pure-reasoning Turn (Tools off, `Invoke-DpEngineCommand`) — no Engine change. 6 new Private helpers (`Get-DpMemoryLimits`, `Import-`/`Save-DpMemoryStore`, `New-DpMemoryPrompt`, `ConvertFrom-DpMemoryResult`, `Get-DpMemoryPayload`); `New-DpTurnParameter -AgentMemory` fenced injection after the preferences block; `Invoke-DpTurn` passes `$script:DeskPilot.Memory.text`; new `memoryLearning` Setting; store loaded at startup; routes `GET`/`PUT /api/memory` + `POST /api/memory/learn` (404/409/`400 too_short`). Frontend (`app.js`): Settings User-profile/Agent-memory fields + char-budget count + learn toggle + `renderMemMeta`/`maybeLearnMemory`; CSS `.mem-row`. +17 unit tests. Fixed a JSON date-coercion bug on load (`ConvertTo-DpIsoString`) the first build caught. Specs 010 (FR-M7 reframe, FR-M12/M13, FR-S1) / 030 (Memory section) / 040 / 050 (T11) / 060 (Phase 2.8) + glossary (Memory / User Profile / Agent Memory + a **Memory vs. the dev `.memory-bank/`** note) + CHANGELOG. Verified: full Sampler build+test **344/344, 0 failed** (16 tasks, 0 errors, 0 warnings); `app.js` ESM check OK. |
| 2026-07-07 | **Hardened `Get-DpAgentList` against an inaccessible agents root** (separate commit on `ai/persistent-memory`). The `Test-Path` probe on the configured Agents root could throw when the root is on a drive/path that denies access (e.g. a restricted mapped drive); added `-ErrorAction SilentlyContinue` so it returns `@()` per its documented contract ("empty when the root is unset or missing"). Surfaced as an environmental test failure on this machine — a restricted `X:` drive throws Access Denied under the build's `ErrorActionPreference=Stop` — pre-existing and unrelated to the memory feature; the fix also greens the build here. |
| 2026-07-07 | **Shipped a Hermes-inspired Memory & context batch on `ai/memory-context-batch` (local-only, not merged).** After the user shared screenshots of a similar tool, migrated two well-fitting ideas. (1) **Automatic conversation compaction**: a new `autoCompaction` Setting (default on) fires the existing `POST /compact` automatically after a Turn once the measured context occupancy (`promptTokens ÷ maxContextWindowTokens`) reaches `compactionThreshold` (0.5–0.95, default 0.8), keeping `compactionKeepRecent` (2–100, default 4) recent messages verbatim. New `maybeAutoCompact()` in `_runTurn`'s finally (mirrors `maybeAutoTitle`; re-entrancy-guarded; `400 too_short` = silent no-op; toasts freed tokens). The compact route now reads `keepCount` from the setting so one knob drives manual + auto. Settings drawer **Memory & context** section + a Session Info "Auto-compaction is on (at N% full)" indicator. Mirrors Hermes's Auto-Compression/Threshold/Protected-Recent. (2) **Usage panel enhancements** (frontend-only, data already tracked): **Tokens in/out** rows on the session + all-time grids, a **Top models (this session)** list (`renderTopModels`), and a **30d** chart range; `Get-DpUsagePayload` daily window 30→60. Deferred (specs/060): Hermes's System screen (live logs + update/restart — needs logging infra), persistent agent memory (Preferences already cover User Profile), Top Skills (Engine doesn't report per-Skill activity). Backend: `Get-DpDefaultSettings` (+3 keys), `Merge-DpSettings` (+3 validated), compact keepCount from settings, `Get-DpUsagePayload` daily 60. +6 unit tests. Specs 010 (FR-C19/FR-U5/FR-S1) / 030 / 040 / 060 (Phase 2.7 + deferred); glossary Auto-compaction row + note; CHANGELOG. Verified: full Sampler build+test **327/327, 0 failed** (16 tasks, 0 errors, 0 warnings); `app.js` ESM check OK (`.mjs`) — the ESM check caught a dropped `stopTurn` declaration mid-edit, fixed before the green run. |
| 2026-07-07 | **Merged `ai/session-info-compact` into `main` (local-only, not pushed).** Fast-forwarded `43da6f5..03e0fb7` — the single feature commit `03e0fb7` (the GitHub-Copilot-style Session Info popover + Compact conversation). Working tree clean; full Sampler build+test was **321/321** green at the branch tip. `ai/session-info-compact` kept (not deleted). |
| 2026-07-07 | **Added a GitHub-Copilot-style Session Info popover + Compact conversation.** Each Conversation now has a **Session Info** panel — opened from a glanceable top-bar **context meter** pill (`#btn-context`) or a **Session info** ⋯-menu entry — showing accumulated **cost** (credits + $) and turn count, a **Context Window** gauge (the last Turn's exact `promptTokens` ÷ the effective Model's `maxContextWindowTokens`, colour-graded, with a hatched *reserved for response* tail = `maxOutputTokens`) and an **estimated** Messages-vs-System+tools split (client-side ~4 chars/token; total measured, split labelled estimated). A **Compact conversation** button calls the new `POST /api/conversations/{id}/compact`: a pure-reasoning Turn (all Tools off, like auto-title/Merge-Plan) summarises the earlier `history` and keeps the last 4 entries verbatim so future Turns replay far fewer tokens — the **visible transcript (`messages`) is preserved**; only the Engine `-History` shrinks. 3 new pure Private helpers (`New-DpCompactionPrompt`, `ConvertFrom-DpCompactionResult`, `Compress-DpConversationHistory` — `[AllowEmptyCollection()]` on the `[object[]]` history params so `@()` binds), a new nullable `compactedUtc` field plumbed through `New-DpConversation`/`Save-`/`Import-DpConversationStore`/`Copy-DpConversation` (reset on copy) + the `list`/`GET conversation` shapes, and route guards (404/409/`400 too_short`/`502 compaction_failed`; `compactedUtc` does not bump `updatedUtc`). Frontend (`app.js`): `computeSessionInfo`/`renderContextMeter`/`fillSessionPopover` (DOM-built, XSS-safe) + gauge/breakdown, wired into the ⋯ menu, header pill, outside-click, and meter refresh on select/home/turn. +16 unit tests. Glossary (Session Info, Context Window, Compact + 3 notes), specs 010 (FR-C18) / 030 (compact route + `compactedUtc`) / 040 (menu + context meter), CHANGELOG. Verified: full Sampler build+test **321/321, 0 failed** (16 tasks, 0 errors, 0 warnings); `app.js` ESM check OK. On `ai/session-info-compact` (local-only, not merged). |
| 2026-07-07 | **Merged `ai/conversation-menu-extras` into `main` (local-only, not pushed).** Fast-forwarded `f50b263..5892e6d` — the extended Conversation action menu (open-in-new-window, unread + mark-all-read, colour, duplicate, copy transcript, details) plus the accumulated-Usage Details follow-up. Working tree clean; full Sampler build+test was **305/305** green at the branch tip. `ai/conversation-menu-extras` kept (not deleted). |
| 2026-07-07 | **Added accumulated Usage to the Conversation Details popover.** The per-Conversation **Details** view (from the ⋯ menu) now shows the total **cost ($), credits, and tokens** for the Conversation, summed across its Messages' per-Turn `usage`. Client-only: new `sumConversationUsage(messages)` in `app.js`; `showConversationDetails` is now async and fetches the full Conversation on open (Details is an explicit action, so the hot list path stays untouched), rendering the three Usage rows as `…` placeholders then filling them in (`—` on a fetch failure). Formatting matches the Usage popover (`$x.xxxx`, `formatCredits`, `toLocaleString`). No backend change (per-Message `usage` already exists). Specs 010 (FR-C17) / 040 (Details) + CHANGELOG updated. Verified: `app.js` ESM check OK; sum logic node-verified (ignores user Messages + missing usage). On `ai/conversation-menu-extras` (local-only). |
| 2026-07-07 | **Extended the per-Conversation ⋯ action menu (8-feature batch, user-approved from a tiered proposal).** Grouped the menu into Open / Organise / Manage sections (with `menu-divider`s) and added: **Open in new window** (`openConversationInNewWindow` → a `/?c=<id>` deep link handled in `enterApp`), **Duplicate** (`POST /api/conversations/{id}/duplicate` + new `Copy-DpConversation` helper — JSON-round-trip deep copy, fresh id, `Copy of ` prefix, flags reset, `titleLocked`), **Mark as unread / Mark all as read** (new `unread` Conversation field; `toggleUnread`; `POST /api/conversations/read-all`; an unread dot + bold title in the list and a "Mark N as read" control), an optional **Colour** label (new nullable `color` field from a fixed palette validated server-side — `400 bad_color`; a swatch row in the menu; a colour dot on the row), **Copy transcript** (`copyTranscript` + extracted `buildTranscript`, reused by export), and **Details** (a read-only popover). Keyboard on a focused row: **Enter** opens, **F2** renames, **Delete** archives (reversible). **Deletion is unchanged** per the user's mid-turn instruction — the hover ✕ and its instant delete stay as-is; Delete is NOT in the menu. `unread`/`color` persist through `New-DpConversation` / `Save-DpConversationStore` / `Import-DpConversationStore` and are carried on the `list`/`search` summaries; `patchConversation` accepts them without bumping `updatedUtc`. +6 unit tests (flag defaults, store unread/colour round-trip, 4× `Copy-DpConversation`). Glossary rows (Unread, Duplicate, Colour) + disambiguation notes (Duplicate vs Branch/Clone; Colour vs the `color` field). Specs 010 (FR-C17) / 030 (routes + fields) / 040 (menu). Verified: full Sampler build+test **305/305, 0 failed** (16 tasks, 0 errors, 0 warnings); `app.js` ESM check OK (`.mjs`). On `ai/conversation-menu-extras` (local-only). |
| 2026-07-07 | **Merged automatic conversation titles into `main` (local-only, not pushed).** Fast-forwarded `b49f959..05a6b9b` — the single feature commit `05a6b9b` (GitHub-Copilot-style auto-titling of a new Conversation from its first prompt). Working tree clean; full suite was **299/299** green at merge. `ai/auto-conversation-title` kept (not deleted). |
| 2026-07-07 | **Added automatic conversation titles (like GitHub Copilot).** A brand-new Conversation was titled with a crude 60-char truncation of the first prompt (inline in `Invoke-DpTurn`); now, after the first Turn, the SPA fires a new best-effort `POST /api/conversations/{id}/title` and the Host Server runs a pure-reasoning Turn with ALL Tools disabled (the Merge-Plan pattern via `Invoke-DpEngineCommand`) to summarise the first prompt into a few words, cleans it, and persists it. Two pure helpers either side of the Engine call: `New-DpTitlePrompt` (strict few-word-title instruction; input truncated to 800 chars) and `ConvertFrom-DpTitleResult` (first meaningful line, skipping a leading `label:` line + code fences; strips a `Title:` label / Markdown heading-list markers / surrounding straight+smart quotes+backticks; collapses whitespace; removes trailing punctuation; caps words 8 then length 60). New route `titleConversation` (`Invoke-DpRouteHandler`): 404 if missing; returns the current title unchanged when `titleLocked` or past the first exchange (>1 user Message); `409` if a Turn runs; else Engine call + clean + persist; never throws (an Engine failure keeps the fallback). New `titleLocked` Conversation field (default `$false`, set `$true` on a manual rename in `patchConversation`) persisted through `New-DpConversation` / `Import-DpConversationStore` / `Save-DpConversationStore` (the store whitelists fields) so auto-titling never clobbers a user's chosen name. Frontend `maybeAutoTitle()` runs in `_runTurn`'s `finally` (after the composer re-enables), gated to `messageCount <= 2`; single-threaded server means no runspace race. The instant truncation stays as the fallback until the AI title arrives. Route registered as `POST /api/conversations/{id}/title` in `Start-DeskPilot`. +17 unit tests (3 `New-DpTitlePrompt`, 11 `ConvertFrom-DpTitleResult`, 3 title-lock). Specs 030 + CHANGELOG updated. Verified: full suite **299/299, 0 failed** (9 tasks, 0 errors, 0 warnings); `app.js` ESM check OK (`.mjs`). On `ai/auto-conversation-title` (local-only). |
| 2026-07-07 | **Merged the "Working…" spinner fix into `main` (local-only, not pushed).** Fast-forwarded `0667920..4896066` — the single amended commit `4896066` (the reduced-motion rotation fix). Working tree clean; `styles.css` parses clean (0 errors). `ai/working-spinner-motion` kept (not deleted). |
| 2026-07-07 | **Fixed: the "Working…" spinner froze into a static ring for users with OS animations off.** The streaming activity indicator (`<span class="spinner">` set in `setStreamingUI`, `web/assets/app.js`) is styled `animation: spin .7s linear infinite`, but the global `@media (prefers-reduced-motion: reduce) { * { animation: none !important; transition: none !important } }` rule in `web/assets/styles.css` disabled EVERY animation — including this functional loading indicator — for anyone with OS animations off (Windows → Accessibility → Visual effects → Animation effects, a very common default), so the donut next to "Working…" sat frozen and read as "nothing is happening". Fix (CSS-only, no JS): (1) keep full rotation for normal users; (2) add a targeted `@media (prefers-reduced-motion: reduce) { .spinner { animation: spin 1s linear infinite !important } }` override (`.spinner` + `!important` beats the blanket `*` rule) so the donut keeps ROTATING for reduced-motion users too — a smooth continuous circle, a touch slower than the .7s default (a first pass pulsed the opacity via `spinner-pulse`, but the user found the flashing ugly and asked for circling, and a smooth spin is calmer/safer than a blink anyway); (3) a small legibility bump to the default spinner (13→14px, a second `border-right-color: var(--accent)` segment so the rotating arc reads clearly, `will-change: transform`). `styles.css` parses clean (language service, 0 errors); no keyframe-name collision (existing `spin`/`merge-spin`/`blink`). The launcher serves the source `web/` directly (`Start-DeskPilot.ps1`: `WebRoot = repoRoot/web`, no built copy), so a browser hard-refresh (Ctrl+F5) shows the fix — no rebuild. On `ai/working-spinner-motion` (local-only). |
| 2026-07-07 | **Merged the four session fixes into `main` (local-only, not pushed).** Fast-forwarded `ac71cec..c89c0c7`: the Stop-button fix (`527155f`), the expired-sign-in re-auth (`87810b7`), the conversation-menu sizing (`4cc2c76`), and the transient session-token-exchange retry (`c89c0c7`). Working tree clean; full suite was **282/282** green at merge. `ai/stop-button-fix` kept (not deleted). |
| 2026-07-07 | **Fixed: a transient 403 on the Copilot session-token exchange failed the whole Turn.** ShellPilot exchanges the cached GitHub token for a short-lived Copilot session token (`Get-ShpSessionToken`) at the start of every Turn; that endpoint intermittently returns 403 (Forbidden), so `Invoke-Shp` throws, `EndInvoke` rethrows, and `Invoke-DpTurn`'s catch surfaced the raw *"Session token exchange failed … 403"* as an error frame — the user had to stop and resend (which worked, because the exchange succeeds on retry). Fix: new `Test-DpTransientEngineError` classifier (403/408/429/5xx, forbidden, timeouts, dropped/refused/reset connections, unable-to-connect; walks the inner-exception chain; deliberately **not** 401 so an expired token is never retried) + a bounded retry in `Invoke-DpTurn` (max 3 attempts, 400 ms × attempt back-off) that fires ONLY when the failure is transient **and** nothing has streamed yet (`$turnState.emitted -eq 0`, a new frame counter incremented in `$emit`, so answer text is never duplicated), with a Stop check on each back-off. +10 `Test-DpTransientEngineError` unit tests. Verified: full suite **282/282, 0 failed** (16 tasks, 0 errors, 0 warnings). On `ai/stop-button-fix` (local-only). |
| 2026-07-07 | **Fixed: the conversation actions menu was hugely oversized.** The per-conversation “…” menu is built with classes `popover popover-menu conv-action-menu` and positioned by JS with an inline `top`/`left` (+`transform:none`). It inherited two base `.popover` rules it never overrode: `width: min(420px,92vw)` (≈420px wide) and `bottom: 92px` — and with an inline `top` **and** a CSS `bottom`, the fixed element stretched to span the whole gap (huge height). Fix (CSS-only, on `.conv-action-menu`, which is last in source order so it wins at equal specificity): `width: max-content` + `min-width: 160px` + `max-width: min(280px,84vw)` to hug the longest label, and `bottom: auto` to stop the vertical stretch. `styles.css` parses clean (language service); no other menu uses that class. On `ai/stop-button-fix` (local-only). |
| 2026-07-07 | **Fixed: an expired GitHub sign-in left the user stuck with no way to re-authenticate.** `authenticated` is derived from the token **file** existing (`Test-Path Engine.TokenPath`), not its validity, so an *expired* token still reported `true`: `init()` entered the app (skipping the sign-in screen), `enterApp → loadModels → Get-ShpModel` then failed (the Engine's `Get-ShpSessionToken` hits GitHub `copilot_internal/v2/token` and gets 401), the `/api/models` route returned a generic `502 engine_unavailable`, and the model dropdown dead-ended at *"(sign in to load models)"*. Worse, the only re-auth entry point (Settings → Re-authenticate → `showAuth` → `/api/auth/start`) silently no-op'd because both DeskPilot's `Invoke-DpAuthFlow` short-circuit **and** ShellPilot's `Initialize-Shp` skip a fresh device flow when a token file already exists. Fix (uses ShellPilot's existing `Initialize-Shp -Force`): (1) new `Test-DpAuthError` classifier walks the inner-exception chain and matches only auth signals (401/403/Unauthorized/Forbidden, "Session token exchange failed", "Token file not found", "Initialize-Shp") — never transient network errors; (2) the `/api/models` catch returns `401 { code: auth_required, reauth: true }` for auth failures (still `502` otherwise); (3) `Invoke-DpAuthFlow` gained `-Force` (skips the stale-token short-circuit and passes `Initialize-Shp -Force`); (4) the `authStart` route forwards `{ force }` from the body; (5) frontend — `api()` now attaches `status`/`code`/`reauth` to thrown errors, `loadModels` opens the re-sign-in overlay on a 401, `showAuth({expired})` forces the flow and swaps in "Your sign-in has expired" wording (new `#auth-subtitle`), `startAuth` posts `{ force }`, and Settings Re-authenticate forces too. +9 `Test-DpAuthError` unit tests. Specs: 030 (auth/status validity note, auth/start `force` body, models `401 auth_required`). Verified: full suite **272/272, 0 failed** (16 tasks, 0 errors, 0 warnings); `app.js` ESM check OK (`node --check` on `.mjs`). On `ai/stop-button-fix` (local-only). |
| 2026-07-07 | **Fixed: the Stop button did nothing.** Clicking **Stop** (or Send with an empty composer while streaming) fires `POST /api/conversations/{id}/stop`, whose handler sets `$state.CancelRequested = $true` — the flag `Invoke-DpTurn`'s poll loop checks to call `[PowerShell].Stop()`. But the Host Server's accept loop is single-threaded and handles each request inline, so a running Turn held the only thread for its whole duration; the stop request sat unaccepted in the TCP backlog until the Turn had already finished on its own (the flag flipped too late). Fix: `Invoke-DpTurn`'s poll loop now services pending connections each iteration via a new `Invoke-DpPendingRequest`, so a concurrent `/stop` is accepted mid-Turn and the very next iteration observes the flag, aborts the Engine pipeline, and closes the stream with an `error` frame (`Turn stopped.`). Supporting changes: new private `Invoke-DpClient` (extracted the accept loop's per-client read → dispatch → close, now shared by the pump and `Start-DeskPilot`); `$script:DeskPilot.Listener` registers the `TcpListener`; the pump no-ops without a listener, uses a short 5s read timeout, and caps at 16 requests/call so a burst can't starve the Turn. +5 `Invoke-DpPendingRequest` unit tests. Specs: 020 (cancellation-on-a-single-thread note + accept-loop bullet), 030 (stop serviced mid-Turn). Verified: full suite **263/263, 0 failed** (16 tasks, 0 errors, 0 warnings). One commit on `ai/stop-button-fix` (local-only). |
| 2026-07-07 | **Fixed: `reasoning_effort` HTTP 400 on Models that don't support reasoning effort.** Reasoning effort is a single global Setting, but support is per-Model (e.g. `claude-haiku-4.5` advertises none). `New-DpTurnParameter` forwarded `-ReasoningEffort` whenever the Setting was truthy regardless of Model, so the Engine sent `reasoning_effort` to a Model that rejected it (`{"code":"invalid_reasoning_effort"}`, HTTP 400) and the whole Turn failed at `EndInvoke`. Fix (defence in depth): (1) the Host Server now caches the `/api/models` capability list on `$script:DeskPilot.Models` + `.DefaultModel`; (2) `New-DpTurnParameter` gained a `-ModelReasoningEfforts` param and forwards `-ReasoningEffort` only when the effective Model advertises the chosen effort (suppresses on empty/unknown \u2014 a global preference stays inert on Models that can't honour it); (3) `Invoke-DpTurn` resolves the effective Model (Conversation \u2192 Settings \u2192 Engine default) and passes its cached efforts; (4) the Settings effort menu (`web/assets/app.js`) is now model-aware \u2014 it offers only the effective Model's advertised efforts + default, refreshes on model change, and explains when a Model supports none. +5 `New-DpTurnParameter` unit tests. Specs: 020 (Turn assembly), 030 (models route caching note), 040 (model-aware effort menu). Verified: full test suite **258/258, 0 failed** (9 tasks, 0 errors, 0 warnings); `app.js` ESM check OK (`.mjs` copy). One commit on `ai/reasoning-effort-model-guard`; fast-forwarded into `main` (`a1df360..9c3c3e8`, local-only). |
| 2026-07-07 | **Fixed: a no-Project Turn silently read files from DeskPilot's launch folder (e.g. its own `.memory-bank`).** `Invoke-DpTurn` only repositioned the long-lived Engine Runspace when a `workspaceFolder` was set, so with no Project selected a Turn ran in whatever directory the runspace was last left in — the folder DeskPilot was launched from (its repo checkout, which has a `.memory-bank/`) or a previously selected Project. An agent following a pre-flight/memory-bank convention therefore probed `Test-Path .memory-bank` and read DeskPilot's own Memory Bank despite no Project being active (the reported confusion). Fix: new private helper `Get-DpEngineWorkingDir` (Workspace Folder when set, else `<dataDir>/workspace` via `Get-DpDataDir`, mirroring `Get-DpUploadDir`); `Invoke-DpTurn` now calls `Set-DpEngineLocation` on **every** Turn through it, making the runspace `$PWD` + `[Environment]::CurrentDirectory` deterministic (no launch-dir or stale-Project leak). +3 unit tests (workspace passthrough; fallback on null/whitespace, `Get-DpDataDir` mocked). Specs: 020-architecture (per-Turn neutral cwd) + 050-security-model (no-Project cwd is a neutral scratch folder — a confinement note). Verified: Unit **241/241, 0 failed**; language-service clean (pre-existing `$routes` warning at test line 71 untouched). One commit on `ai/no-project-cwd-leak`; fast-forwarded into `main` (`4dc9e63..0d75a87`, local-only). |
| 2026-07-07 | **Attaching files no longer requires a selected Project.** The composer Attach button + drag-and-drop previously toasted "Select or register a project before attaching files." and the `uploads` route returned `400 no_workspace` when no Workspace Folder was active — contradicting the same-day Close Project feature (no-project is now legitimate). New private helper `Get-DpUploadDir` resolves the upload target: the Workspace Folder when set, else `<dataDir>/uploads` (via `Get-DpDataDir`); the route handler dropped its 400 guard and creates the fallback dir via the existing `Test-Path`/`New-Item` block. Frontend (`web/assets/app.js`): removed the no-project guard in `uploadFiles`; the send-time attachment note names the uploads' **absolute paths** when there is no Workspace Folder (the agent's cwd is not the upload dir) and keeps relative-names-in-the-Workspace-Folder when a Project is active. +3 `Get-DpUploadDir` unit tests (workspace passthrough; fallback on null; fallback on whitespace). Specs: FR-C9 (010), File uploads (020), Uploads (030 — no longer 400 on no Workspace Folder), composer Attach copy (040). Verified: Unit **240/240, 0 failed**; `app.js` ESM check OK (`.mjs` copy); PSScriptAnalyzer clean on the new helper + edited handler; language-service clean (pre-existing `$routes` warning at test line 71 untouched). One commit on `ai/attach-without-project`. |
| 2026-07-07 | **Added a way to close (deselect) the active Project.** Previously the user could only switch Projects or create a new one — there was no path back to a no-project state. The backend already supported it (`Merge-DpSettings` accepts `selectedProjectId: null` and derives a null `workspaceFolder`), so this is UI + specs only, no `.ps1` behaviour change. Frontend (`web/assets/app.js`): new `closeProject()` (= `selectProject(null)`); a **✕ Close project** entry in the composer project popover shown only when a Project is active; a **Close** button on the selected row of the Settings-drawer Projects manager. Closing keeps the Project registered (distinct from **Remove**, which unregisters) and the closed state persists via the existing settings store; the file explorer collapses and File/Terminal Tools fall back to no default folder. Specs: `FR-M2b` (010-requirements), composer + Settings copy (040-ui-design), and the `selectedProjectId: null` close semantics (030-api-contract). Glossary: a new **Close (a Project)** row + a Close-vs-Remove-vs-switch note. +1 `Merge-DpSettings` regression test (close clears selection + `workspaceFolder`, keeps the Project registered). Verified: `app.js` ESM check OK (`.mjs` copy); helper unit suite **235/235, 0 failed**; edited files clean in the language service. One commit on `ai/close-project`. |
| 2026-06-13 | **File explorer now auto-refreshes.** Made `refreshExplorer` state-preserving and silent-capable: it tracks expanded folders in `state.explorerExpanded` (absolute paths) and re-opens them after a rebuild, preserves scroll position, and (when `{ silent: true }`) keeps the current tree + Git bar visible while rebuilding off-DOM then swaps atomically (no flicker). `buildDirNode` is now async and auto-expands folders in the tracked set; `refreshGitBar(silent)` skips the “Checking Git…” flash. Added `wireExplorerAutoRefresh`: silent refresh on window `focus`, on `visibilitychange`→visible, and every 5s while the explorer is open + the tab is visible — guarded to skip while `document.activeElement` is inside the explorer (so a poll never closes an open branch dropdown or yanks the tree mid-interaction). Overlap-safe via a seq + busy guard (a newer refresh wins the DOM swap; silent polls skip while busy). The expanded set clears on project switch / no-project. UI-only; `app.js` parses as an ES module; no `.ps1` touched. Also fixed earlier: the “Merge into <default>” button hides when the default branch is current. |
| 2026-06-12 | **Shipped the Branch Merge Wizard end to end (routes + UI) on top of the backend.** Added 8 HTTP routes (`GET /api/git/branches`, `GET /api/git/merge/preview`, `POST /api/git/merge`, `/merge/plan`, `/merge/apply`, `/merge/abort`, `/merge/undo`, `/cleanup`) in `Start-DeskPilot` + `Invoke-DpRouteHandler` (each confined to the Project, no-workspace guard; the conflict `/merge/plan` route runs a pure-reasoning `Invoke-Shp` Turn with ALL Tools disabled via `Invoke-DpEngineCommand`, guarded by `$state.TurnRunning` -> 409). Frontend (`web/`): the explorer Git bar branch picker now shows a merged (`✓`) / not-merged (`❗`) badge per branch with hover tooltips + an `ⓘ` legend and lists remote-only branches (marked), plus a `Merge into <default>…` button opening a step-by-step Merge Wizard modal (choose branch -> preview incoming commits + dirty/behind preconditions with an auto-fix checkbox -> merge ff-or-commit -> on conflict, `Ask the AI` -> a reviewable Merge Plan with per-file content + binary keep-ours/theirs radios -> `Apply` -> result -> local cleanup, with remote push+delete behind a second `window.confirm`; `Undo this merge` throughout). `specs/030` documents every route. Verified: full Sampler build green TWICE (246 tests, 0 failed, 0 errors, 0 warnings); `app.js` parses as an ES module (`.mjs` check, per the recorded ESM rule); HTML/CSS/JS + all `.ps1` clean; route-table names match handler `switch` cases 1:1 (8/8). Three commits on `ai/merge-wizard` (`50b4ca5` foundation, `c84f628` backend +33 real-repo tests, `0612594` routes+UI). Remaining: a live HTTP smoke of the routes (helpers already real-repo-tested) + the Clone Wizard (spec 080). |
| 2026-06-12 | **Started the Branch Merge Wizard (specs signed off + backend foundation).** A grill-me interview produced two signed-off Design Concepts, persisted as `specs/070-merge-wizard.md` (Merge Wizard) and `specs/080-clone-wizard.md` (Clone Wizard); added 7 glossary rows (Branch, Default Branch, Merge, Merge Wizard, Merge Plan, Clone, New Project Wizard) + boundary notes. Shipped the backend foundation (process-based via `Invoke-DpGitCommand`, confined to the Project, never-throw): `Get-DpDefaultBranch` (origin/HEAD -> main -> master), `Invoke-DpGitFetch` (best-effort; reports no-remote/offline), `Get-DpBranchList` (local + remote-only Branches with a merged-into-Default flag, optional pre-fetch, a comparison ref that exists locally or as origin/<default>). +13 unit tests (mock Invoke-DpGitCommand/Get-DpGitStatus/Invoke-DpGitFetch); helpers suite 172/172 green. Key decisions: merge target = the Default Branch; the AI proposes a Merge Plan (pure-reasoning Turn, Tools off) the user approves before any write; ff-else-merge-commit; autofix stash->ff->merge->pop; auto local cleanup + remote push/delete behind a SEPARATE confirm; ambient git credentials only; Undo resets the default to the pre-merge sha. Remaining: merge preview/exec/conflict/abort/undo, cleanup, the AI plan Turn, routes (+specs/030), and the wizard UI. |
| 2026-06-12 | **Reverted the CI to faithfully mirror ShellPilot's pipeline (per user direction).** A prior iteration had diverged from ShellPilot (two trigger-split deploy jobs `publish_prerelease`/`publish_release`, a separate `smoke` job, a `PUBLISH_ENABLED` gate variable, and `GitVersion` `ContinuousDeployment`). Since ShellPilot already "does it as needed" with a single `deploy` job, restored exactly that shape: `build -> test -> deploy` (deploy `needs: [build, test]`, runs on push to `main` or a `v*` tag for owner `raandree`, `./build.ps1 -Tasks publish` + `Create_ChangeLog_GitHub_PR`); reverted `GitVersion.yml` to `ContinuousDelivery`; matched the test-results artifact name (`CodeCoverage-<os>`). Prerelease-on-main / stable-on-tag is produced by Sampler/GitVersion from the computed version; publishing stays dormant until the `GalleryApiToken`/`GitHubToken` secrets are set (the publish tasks self-guard), which is the go-live gate instead of a custom variable. Verified: both YAMLs parse clean (no BOM, LF) and DeskPilot's job set + deploy condition now match ShellPilot's exactly (build,test,deploy; needs build,test; identical if; ContinuousDelivery). |
| 2026-06-12 | **Defined the release model in CI: prerelease per main commit + full release on a `v*` tag.** Split the single deploy job in `.github/workflows/ci.yml` into two trigger-scoped jobs (both still gated OFF behind `PUBLISH_ENABLED`): `publish_prerelease` (on push to `main`, runs only `./build.ps1 -Tasks publish_module_to_gallery` -> a PowerShell Gallery prerelease, `Install-Module -AllowPrerelease`; no GitHub release, no changelog cut) and `publish_release` (on a pushed `v*` tag, runs `./build.ps1 -Tasks publish` + `Create_ChangeLog_GitHub_PR` -> GitHub release + stable Gallery module + changelog PR). Verified by reading the real Sampler task source: `publish_module_to_gallery` is prerelease-aware via the manifest `Prerelease` field stamped by GitVersion; `Publish_release_to_GitHub` does NOT skip prereleases (it would create `v$ModuleVersion` on every commit), which is why the prerelease path deliberately calls only the gallery task. Switched `GitVersion.yml` from `ContinuousDelivery` to **`ContinuousDeployment`** so each main commit yields a unique, monotonic prerelease number (`-preview0009`, `-preview0010`) instead of a build-metadata-only difference the Gallery strips (which would collide). A `v*` tag still produces a clean stable version. Both YAML files parse clean (powershell-yaml); no local dotnet so the GitVersion numbers themselves verify only in CI (gated off). Note: ShellPilot likely shares the ContinuousDelivery prerelease-collision limitation if it wants per-commit previews. |
| 2026-06-12 | **Added a GitHub Actions CI pipeline (Phase C, dry-run).** New `.github/workflows/ci.yml` mirroring ShellPilot's `ci.yml`: a **build** job (GitVersion 5.x via `dotnet tool`, `./build.ps1 -ResolveDependency -Tasks pack`, upload `output/` artifact, version in the step summary + job outputs), a **test** matrix (ubuntu/windows/macos, PowerShell 7; download artifact; `./build.ps1 -Tasks test`; upload testResults), a new **smoke** job (download artifact, import the built `DeskPilot.psd1`, assert `Start-DeskPilot` is exported), and a **deploy** job (`./build.ps1 -Tasks publish` + `Create_ChangeLog_GitHub_PR`) that `needs: [build, test, smoke]`. Triggers: push to `main`, `v*` tags (not `v*-*`), PRs to `main`, `workflow_dispatch`; `paths-ignore: CHANGELOG.md`. **Deploy is gated OFF** behind the repo variable `PUBLISH_ENABLED == 'true'` (plus `repository_owner == 'raandree'` + main/tag) so it never publishes until go-live (Phase D) — flip by setting the variable + adding `GitHubToken`/`GalleryApiToken` secrets, no code change. Smoke is import-only for now; the full server `/api/health` smoke is deferred to Phase B (needs web bundled + a module-relative `-WebRoot`). Verified: UTF-8 no BOM, trailing LF, parses as valid YAML (jobs build/test/smoke/deploy; deploy needs build,test,smoke). Branch protection (PR-only + green-CI-to-merge) remains a GitHub-UI setting to enable after the first PR run. |
| 2026-06-12 | **Converted to a Sampler build project (Phase A of the publish/Sampler plan).** Moved module source `src/DeskPilot/` → `source/` (empty psm1 + `Prefix.ps1` carrying `Set-StrictMode -Version Latest`); manifest version → `0.0.1` placeholder (GitVersion owns it). Added lean Sampler scaffolding: `build.ps1` + `Resolve-Dependency.ps1`/`.psd1` copied from ShellPilot; `build.yaml` (CopyPaths empty for now — web bundling is Phase B), `RequiredModules.psd1` (incl. ModuleBuilder's `Configuration`/`Metadata` deps), `GitVersion.yml` with `main` as mainline. Tests split into `tests/QA/module.tests.ps1` (mirrored from ShellPilot; per-function gates scoped to the **exported** surface because DeskPilot has one public command + ~60 collectively-tested Private helpers) and `tests/Unit` (helpers test repointed to `source/Private`; new `Start-DeskPilot.Tests.ps1`). Made `Start-DeskPilot` PSSA-clean (ASCII-only + ShouldProcess/Write-Host suppressions). Rebuilt the launcher to import the built module (auto-build on first run). Verified: `./build.ps1` build 0 errors; **207 tests pass, 0 failed, 0 skipped**. Publish stays gated OFF until Phase D. |
| 2026-06-12 | **Engine distribution: download ShellPilot from the PowerShell Gallery.** Now that ShellPilot is published, replaced the hardcoded local-build probe (`V:/Git/ShellPilot/output/module/ShellPilot`) and the MyDocuments probe with a new `Resolve-DpEngineModule` helper: resolve an explicit `-EngineModulePath`, else an already-installed `ShellPilot` on `PSModulePath` (newest version), else `Install-Module ShellPilot -Scope CurrentUser -AllowPrerelease -Force` then re-resolve — returning `{ Path; Installed; Error }` without importing (`-StableOnly`/`-SkipInstall` switches included). Rewrote `Initialize-DpEngine` to call it (preserving a resolution error through the import-by-name fallback, adding an `Installed` flag) and surfaced the download in `Start-DeskPilot`'s console output. Added 9 unit tests (mock `Get-Module`/`Install-Module`); fixed a Pester mock-order gotcha by importing PowerShellGet in `BeforeAll`. Updated README, techContext, specs 020/030, CHANGELOG. Verified: both `.ps1` parse clean; Pester 195/195 (+9). | The Engine made its built-in `manage_todo_list` tool on by default and replaced the opt-in `-EnableTodoList` switch with an opt-out `-DisableTodoList` (ShellPilot commit `3c0fc6e`). DeskPilot still passed `-EnableTodoList` whenever `taskTracking` was on (the default), so `Invoke-Shp` rejected the now-unknown parameter and **every Turn failed** against the current Engine. Inverted the mapping in `New-DpTurnParameter` (pass `-DisableTodoList` only when `taskTracking` is off; otherwise rely on the Engine's default-on behaviour), rewrote the two Task List splat tests, and updated the glossary Engine-boundary note. Confirmed via `git diff 3f60386 HEAD -- source` that this rename is the **only** functional ShellPilot source change since its init commit: the `-ShowThinking` streaming fix needs no DeskPilot change (the `Get-DpStreamFrame` classifier already routes the DarkGray `thinking:` label + ANSI `3;90m` reasoning + DarkCyan/Cyan/Yellow trace, and the fix actually keeps live streaming on when thinking is enabled), and the `ShellPilot.Result` default-view fix is display-only (DeskPilot reads `.Content`/`.History`/`.Usage`/`.TodoList` programmatically). Verified: both edited `.ps1` parse clean; Pester 186/186. |
| 2026-06-08 | Memory Bank established (brief, product context, tech context, system patterns, glossary, active context, progress). |
| 2026-06-08 | Seven specs written (overview, requirements, architecture, API contract, UI design, security model, roadmap). |
| 2026-06-08 | Host Server module `src/DeskPilot` implemented: loopback HTTP + SSE, token gate, static serving, REST + streaming routes, Engine runspace, Conversation store, Settings, Usage. |
| 2026-06-08 | Build-free Claude-style SPA `web/` implemented with streaming, Activity, Usage, Permissions, Settings, and device-code auth; XSS-safe Markdown renderer. |
| 2026-06-08 | Launcher `Start-DeskPilot.ps1` + `DeskPilot.cmd`; README + CHANGELOG. |
| 2026-06-08 | Verified: AST parse clean; 28/28 Pester tests pass; live end-to-end smoke against the real Engine (auth, CRUD, settings validation, security gates, static serving). |
| 2026-06-09 | Live end-to-end test against real Copilot passed: streaming Turn (SSE start/delta/done + cost), multi-turn history via -History, tool-using Turn (file read shown in Activity), Usage accrual. Added `tests/live/Invoke-DeskPilotLiveTest.ps1`. |
| 2026-06-09 | Fixed StrictMode bug in `ConvertFrom-DpEngineResult` (empty `@(...) \| ForEach-Object` collapsed to `$null`, `.Count` threw) on tool-less Turns; added regression test. Unit suite 29/29. |
| 2026-06-09 | Restyled the UI to a deep-teal / petrol identity (away from Claude warm-clay): cool light theme, dark blue-green dark theme, vivid teal accent, new `--on-accent` token, humanist-sans reading font, updated favicon. Verified served live; refreshed UI-design spec palette and removed stale "Claude-style" descriptors. |
| 2026-06-09 | Fixed Workspace Folder not being honored: inject a `-SystemPrompt` naming the working directory (`New-DpTurnParameter`) and add `Set-DpEngineLocation` to create the folder and set runspace `$PWD` + `[Environment]::CurrentDirectory`. Added a live regression (TEST4) and 2 unit tests; live-verified the agent writes into the configured, auto-created folder without being told the path. Unit suite 31/31; 4/4 live tests pass. |
| 2026-06-09 | Added cross-session persistence: Conversations save to `conversations.json` and load on startup (FR-C7/C8); a lifetime Usage counter (`lifetime-usage.json`) accrues across sessions with a manual reset (FR-U3/U4) plus the session counter (FR-U2). New `POST /api/usage/reset`; `GET /api/usage` returns `{session, lifetime, byModel}`; UI usage popover with both counters + reset. Fixed JSON date coercion via `ConvertTo-DpIsoString`. Unit suite 39/39; 6/6 live tests incl. restart-persistence + reset. |
| 2026-06-09 | Persisted Settings across sessions (`settings.json`, FR-S1): `Save-DpSettings`/`Import-DpSettings`, written on `PUT /api/settings`, loaded in `Start-DeskPilot`. Added file uploads (FR-C9): `POST /api/uploads` (multipart/form-data) with a pure-PowerShell byte-safe parser (`Read-DpMultipartParts`, `Get-DpMultipartBoundary`), collision-safe naming (`Get-DpUniqueFilePath`), 25 MiB cap; saves into the Workspace Folder and the agent reads via its file tool. UI Attach button + pending-attachment chips. Unit suite 49/49; 8/8 live tests incl. settings-persist + upload (agent read the uploaded file and echoed the marker). |
| 2026-06-09 | Fixed Ctrl+C not stopping the Host Server: the accept loop blocked on synchronous `AcceptTcpClient()`, which PowerShell cannot interrupt with a pipeline stop. Loop now polls non-blocking `Pending()` and yields via the `Start-Sleep` cmdlet (a cancellation checkpoint) when idle, so Ctrl+C unwinds within ~50 ms; added a closing "DeskPilot stopped." line. Verified: AST parse; a Start-Job/Stop-Job equivalent of Ctrl+C honored the stop in 24 ms; serve smoke `status=ok`; unit suite 49/49. |
| 2026-06-09 | Fixed the all-time credit counter appearing stuck at 0: the sidebar chip showed only the per-session counter (resets each launch); the persisted lifetime total was visible only in the popover. Chip now shows `session / all-time cr`. Diagnosed the backend as correct (a real Turn moved persisted lifetime 1.6602 → 1.8233; API returns it; accrual + load verified), so the fix is UI-only. |
| 2026-06-09 | Added Projects (FR-M2/M2a): registered named working folders `{id,name,path}` in settings (`projects` + `selectedProjectId`), with `workspaceFolder` derived from the selection and kept in sync (Turn/Upload code unchanged). New `ConvertTo-DpProject`; `Merge-DpSettings` validates selection (400 on unknown), clears stale selection on removal, migrates legacy `workspaceFolder`. UI: composer Project dropdown + ＋Project, settings-drawer Projects manager (select/remove/add). Turn system prompt names the active Project. Glossary + specs updated. Unit 56/56; live 8/8 incl. TEST4b (derived workspace), TEST4 (agent wrote into the Project folder), TEST7 projectKept across restart. |
| 2026-06-09 | Restyled the Project picker: replaced the native `<select>` (whose OS-rendered option list clashed with the design) with a custom popover menu reusing the app's `.popover` pattern and design tokens. New `btn-project` chip (folder + name + caret) toggles a `#project-popover` menu anchored to the button (transform cleared, viewport-clamped) listing projects with a check on the selected one, a divider, and New project…. Verified: `node --check` clean, no language-service errors, served assets confirmed. |
| 2026-06-09 | Added a folder picker for registering a Project (replaces the bare path prompt): styled modal with drive switcher, editable breadcrumb path, Up/Home, scrollable sub-folder list and New folder. New backend `Get-DpDirectoryListing` (lists sub-folders, hidden/system skipped, home fallback) + `New-DpDirectory` (single-segment, rejects separators/`..`); routes `GET /api/fs/list` and `POST /api/fs/mkdir`. Settings-drawer add row is now a single Add project… button opening the same picker. Unit 66/66; fs endpoints verified live (list home/repo, mkdir, 400 on traversal); served assets confirmed. |
| 2026-06-09 | Added an Agent picker: composer dropdown lists `*.agent.md` personas under an Agents folder (default `~/.copilot/agents`); selected Agent's body becomes the Turn system prompt (composed before the Project note). New `Get-DpCopilotDefaults` (derives skills/instructions/agents roots from ~/.copilot), `Read-DpAgentFile` (frontmatter+body), `Get-DpAgentList` (pipe-safe), `Get-DpAgentSystemPrompt`; `GET /api/agents`; settings `agentsRoot`/`selectedAgent`; skill/instruction roots default from ~/.copilot. Also enforced project de-duplication (no duplicate name or path) in Merge-DpSettings + UI guard. Glossary/specs updated. Unit 82/82; agents endpoint verified live (11 personas). |
| 2026-06-09 | Added a collapsible project file explorer (right panel): ☰ Files toggle opens a tree of the selected Project's folders/files with lazy-expanding dirs + sizes; collapsed and disabled when no Project is selected; refreshes after each Turn. New `Get-DpDirectoryEntries` (folders then files, hidden/system skipped, confined to the Project root — escape attempts refused); `GET /api/fs/tree` (400 when no Project). Verified live (repo root 14 entries, src lazy-expand, no-project 400, traversal blocked); node --check clean. |
| 2026-06-09 | Hardened the sign-in flow: `Invoke-DpAuthFlow` now guards against the Engine not being imported (clear message instead of a missing-command error) and short-circuits to success when a token already exists (e.g. user ran Initialize-Shp in a terminal). Auth overlay shows engine state + disables Connect when unloaded, and adds an "I've signed in elsewhere" re-check (recheckAuth) so external sign-in is picked up without restart. Verified live: auth/start emits a single done event when authed. |
| 2026-06-09 | Added settings backup/restore: `GET /api/settings/export` (wrapper `{type,version,exportedUtc,settings}`) and `POST /api/settings/import` (restores by merging onto defaults = full replace; strips version + derived workspaceFolder). UI Back up / Restore buttons in the settings drawer (client downloads JSON / reads a file and re-syncs perms, projects, agents, explorer). Unit 84/84; round-trip verified live incl. bare-object import; regression-checked that the usage handler still works after an editing mishap. |
| 2026-06-09 | Fixed browser file uploads failing with 401 "Missing or invalid session token": the composer upload `fetch('/api/uploads')` omitted the `X-DeskPilot-Token` header (can't use the JSON `api()` helper, which would mangle the multipart body). Now sends the token header without overriding the multipart Content-Type. Verified live: with-token 200 + file written, without-token 401. (The live harness sends the token over a raw socket, so it never caught the browser path.) |
| 2026-06-09 | Fixed ugly live streaming output. Root cause was in DeskPilot, not the Engine: under `-ShowThinking` the Engine emits a documented host-only colour trace (DarkCyan `=== iteration ===` banners, Cyan `-> tool(...)` traces, Yellow notes); DeskPilot's emit handler only classified DarkGray as reasoning, so the trace leaked into the answer delta. Now routes DarkCyan/Cyan/Yellow + `=== iteration`/`-> ` patterns to the reasoning (Thinking) channel; only uncoloured answer tokens stream as delta. Also render the live delta as Markdown (rAF-throttled) so it matches the final. Verified live (showThinking on, tool-using prompt): delta clean of banners/traces, reasoning has them, answer correct + markdown. Unit 84/84. |
| 2026-06-09 | Fixed credit float drift and added a credits-per-day graph. Drift: session/lifetime/byModel accrued raw per-Turn credits without rounding (e.g. 0.6174999999999999, 2.3987000000000003); now rounds credits to 4 dp + cost to 6 dp on every accrual, heals persisted values on load. byModel now carries credits+turns. New `Add-DpDailyUsage` + `ConvertTo-DpDayKey` (per-day UTC buckets, drift-safe, JSON-round-trip-safe, 60-day retention) persisted in lifetime-usage.json; `GET /api/usage` exposes `daily` (last 30d). UI: inline SVG bar chart in the usage popover with a 7d/14d toggle (no chart library). Found + fixed a PowerShell case-insensitive-variable bug ($date aliased the [datetime]$Date param). Unit 92/92; live serve-verified daily+byModel populated. |
| 2026-06-09 | Fixed the credits popover opening empty: it was revealed before being populated (populate was guarded on the popover being visible, which it wasn't yet on open), so grids + chart only appeared after clicking 7d/14d. The usage-chip onclick now calls populateUsagePopover from fresh data before removing the hidden class; 14d is the default chart view. Served-verified. |
| 2026-06-09 | Added shell-style prompt history in the composer: ↑ recalls previous prompts, ↓ moves toward newest, draft preserved + restored past the newest. Seeded from the current Conversation's user messages (seedPromptHistory on select), grows on send (recordPrompt, dedups consecutive). Up gated to first line / Down to last line so multi-line editing is unaffected; typing exits navigation. Logic unit-simulated in Node (recall, clamp, draft restore, empty no-op); served-verified. |
| 2026-06-09 | Added Git awareness to the file explorer: a Git bar shows whether the selected Project folder is a repo (warning + git init button if not; current branch + branch-switch dropdown if so), and switching a branch refreshes the tree. New `Invoke-DpGitCommand` (process-based, no shell, captures exit/stdout/stderr) + `Get-DpGitStatus` (gitAvailable/isRepo/branch/detached/branches/root). Routes GET /api/git/status, POST /api/git/init, POST /api/git/checkout (confined to workspaceFolder; checkout validated against live branch list, 409 on failure). Unit 96/96; end-to-end serve-verified (non-repo -> init -> branch list -> checkout -> 400 unknown). |
| 2026-06-09 | Fixed the file explorer not refreshing when switching projects: `syncExplorerAvailability` (the chokepoint all project changes flow through) now reloads the tree + git bar when the open explorer's tracked path (`state.explorerPath`, set on each refresh) differs from the newly selected project's workspaceFolder. node --check clean; served-verified. |
| 2026-06-09 | Added a file viewer to the explorer: clicking a file opens a modal that renders Markdown (Rendered/Raw toggle, reusing the chat's `.content` styles + code-copy buttons), shows other text as a plain `<pre>`, and reports size + preview truncation; non-text files say they can't be previewed. New `Get-DpFileContent` (UTF-8 read confined to the Project root — escape refused; 1 MiB preview cap with `truncated`; NUL-byte binary sniff; BOM strip); route `GET /api/fs/file` (400 on no project / no path). File rows are now `<button>`s. Unit 103/103 (+7); end-to-end serve-verified (README 200 + rendered, outside-root refused, no-path 400, no-token 401). |
| 2026-06-10 | Restyled the browser scrollbars to match the deep-teal / petrol UI (were the raw OS scrollbar). Added `--scrollbar-track` / `--scrollbar-thumb` / `--scrollbar-thumb-hover` tokens to the light + both dark theme blocks and a global rule: a slim rounded pill thumb (transparent-border + `background-clip:padding-box`) in muted petrol that brightens on hover, transparent track. Covers WebKit (`::-webkit-scrollbar*`) and Firefox (`scrollbar-width`/`scrollbar-color`); theme-aware. CSS only — no errors from the language service. |
| 2026-06-10 | Added a quick light/dark toggle to the top bar: a sun/moon `#btn-theme` icon-button flips between light and dark in one click. New `effectiveTheme()` resolves `system` against `prefers-color-scheme` so the toggle always lands on the opposite mode; `applyTheme()` now refreshes the icon; `toggleTheme()` persists `ad_theme` and syncs the Settings `set-theme` dropdown. A `matchMedia` change listener keeps the icon honest when the OS theme flips while on `system`. `node --check` clean; no language-service errors. |
| 2026-06-10 | Added an in-Turn Task List (parallel to VS Code Copilot's `manage_todo_list`): when the new `taskTracking` Setting is on (default), `New-DpTurnParameter` passes `-EnableTodoList`; `Invoke-DpTurn`'s emit pipeline routes every `Streams.Information` record through new `Get-DpStreamFrame`, which classifies ShpProgress records (`Kind=TodoList` → `tasks` SSE frame after `ConvertTo-DpTaskList` re-normalisation; `Kind=ToolCall` absorbed; unknown Kind ignored) and falls non-ShpProgress records through to the existing trace/delta path; `ConvertFrom-DpEngineResult` maps `result.TodoList` to the assistant Message's `tasks` field, persisted by the existing store. Web UI adds a compact Tasks panel (○ ◐ ✓ glyphs, `Tasks — completed/total` header, idempotent full-list replace) above Activity, plus a Settings checkbox. Glossary rows added (Task / Task List); third-party `manage_todo_list` / `TodoList` kept verbatim only at the Engine boundary; `-ShowThinking` marker-string parsing explicitly forbidden. Specs 020/030/040 updated. Unit 123/123 (+20: 9 normaliser, 6 stream-classifier, 2 mapper, 2 splat, 1 default); PSScriptAnalyzer warnings unchanged from baseline. |
| 2026-06-10 | Added mid-Turn dispatch in the composer: a chevron next to **Stop** opens a popover with **→ Stop and Send** (calls `/stop`, awaits SSE close, then sends a fresh Turn), **+ Add to Queue** (Alt+Enter, buffers the prompt and fires it after the current Turn ends) and **↑ Steer with Message** (Enter, the recommended default — same as Queue, but the prompt sent to the Engine is prefixed with a steering preamble so the Model treats it as a course correction). Pending entries show as chips above the textarea with `×` to discard; user bubbles get a `Steered mid-turn` / `Sent from queue` badge. Purely client-side: `/messages` and `/stop` wire contracts unchanged. New helpers `openDispatchPopover`, `dispatchEnqueue`, `dispatchStopAndSend`, `flushDispatchQueue`, `renderDispatchQueue`; `state.dispatchQueue` and a `state.streamEndPromise` resolved by the Turn finally-block. Specs 030/040 document the convention. Verified: `node --check web/assets/app.js` clean; Pester 123/123 still green; PSScriptAnalyzer warnings unchanged. |
| 2026-06-10 | Added CopilotAtelier references (README, `.memory-bank/projectbrief.md`, `specs/000-overview.md`) framing the sibling repo as the canonical Atelier exemplar that DeskPilot's `~/.copilot/*` discovery is designed to consume; added `docs/logo-prompt-m365-copilot.md`, a copy-paste prompt for Microsoft 365 Copilot / Designer that pins the deep-teal palette, the 16 px / 20 × 20 px sizing constraints and asks for a logo system that ShellPilot and CopilotAtelier can slot into. |
| 2026-06-10 | Added a Customizations surface (manage AI resources): a 🧩 sidebar modal lists agent-shaping Markdown files under the configured roots in four categories — Agents (`*.agent.md`), Skills (`SKILL.md`), Instructions (`*.instructions.md`), Prompt files (`*.prompt.md`) — grouped by User/Workspace scope with counts + search, and a line-numbered editor (Edit/Preview, Save/Ctrl+S, dirty-guard, scaffolded ＋New). New backend `Get-DpCustomizationCatalog` (four-category source of truth), `Get-DpCustomizationRoot`, `Get-DpCustomizationList`, `Resolve-DpCustomizationPath` (single security gate: descendant-of-configured-root + filename-pattern), `Get-DpCustomizationContent` (1 MiB cap/BOM strip/binary sniff), `Save-DpCustomizationContent` (edit-only, atomic temp+Move, UTF-8 no BOM), `New-DpCustomization` (single-segment name validation, scaffold, refuse-existing); routes `GET`/`POST /api/customizations` + `GET`/`PUT /api/customizations/content`. Added a `promptRoots` Setting (default `~/.copilot/prompts`) end to end. Saving an Agent refreshes the composer Agent menu. Specs 010/020/030/040/050 + glossary updated (new Customization / Prompt File terms). Unit 153/153 (+30); live HTTP test of all four endpoints incl. traversal→blocked, save-outside→400, no-token→401; `node --check` clean; PSScriptAnalyzer unchanged from the baseline `New-*` pattern. |
| 2026-06-10 | Revised `docs/logo-prompt-m365-copilot.md`: the Microsoft 365 Copilot brief now asks for **5** proposals (was 4), foregrounds the **Agentic Operating Model** ("friendly front door to the AOM") and the literal **ShellPilot** cockpit-vs-engine relationship, adds a **Where the logo will live** section pinning the two homes (GitHub README hero + 1280×640 social card; the DeskPilot entry-page 20×20 brand swatch + 16×16 favicon + start-screen splash), expands to 5 concept directions, and revises deliverables for both placements with light/dark variants. Docs-only; no code or tests changed. |
| 2026-06-10 | Integrated the new compass-**D** brand logo. Copied two exported assets into `web/assets` (`logo-mark.png` — the teal mark matching `#0d9488`; `logo-full.png` — the full lockup with the *Navigate with clarity.* tagline) and wired them in: PNG favicon, sidebar brand, empty-state hero, and assistant-message avatar use the mark; the sign-in card uses the lockup; the README now leads with the lockup. Replaced the CSS-drawn letter mark + inline-SVG favicon; added `.brand-mark-img*` / `.auth-logo` CSS. The static server already serves `.png` (`image/png`). Verified: `node --check` clean; HTML/CSS/JS language-service clean; markdownlint clean after switching the README hero to a pure-Markdown image (MD033/MD041). No `.ps1` touched, so Pester 153/153 unaffected. |
| 2026-06-10 | Reworked the brand logo for quality + theme fit. The exported art had an opaque white background (24bpp, no alpha), so on the dark UI the sidebar/avatars showed a white tile, and a dark-teal mark would be low-contrast on dark anyway. Regenerated four transparent, theme-aware PNGs from the source via a .NET un-matte (System.Drawing LockBits: alpha from per-pixel whiteness, optional ink recolour): `logo-mark.png` (teal) + `logo-mark-dark.png` (bright teal `#2dd4bf`) at 256², and `logo-full.png` (two-tone) + `logo-full-dark.png` (bright teal) at 680×453. Switched the brand marks to `background-image` spans and the auth lockup to paired `<img>`s, each swapped by `:root[data-theme="dark"]` + a `prefers-color-scheme` rule for `system`; theme-aware favicon via media-query `<link>`s. Shrank the README hero to a small top-right corner mark via `<picture>` (theme-aware) with a scoped `markdownlint-disable` for MD033/MD041. Verified: all four PNGs 32bpp ARGB, corner A=0; `node --check` + HTML/CSS/JS + markdown language-service all clean; no `.ps1` touched (Pester 153/153 unaffected). |
| 2026-06-10 | Made the sidebar DeskPilot brand a home button: clicking it clears the open Conversation (`state.current = null`) and renders the empty "How can I help?" home screen, deselecting the active list item; the next sent message starts a fresh Conversation (existing `send()` path). New `goHome()` (guards against mid-stream nav with a toast) wired in `wireGlobal`; brand markup is now a `<button id="btn-home">` with `.brand-home` button-reset CSS (left-aligned, subtle hover, focus-visible ring). Verified `node --check` + HTML/CSS/JS language-service clean; no `.ps1` touched (Pester 153/153 unaffected). |
| 2026-06-11 | Shipped a knowledge-worker quality-of-life batch (Phase 2.5). Frontend (client-side, landed earlier): voice dictation + read-aloud + per-message copy, sandboxed `html`/`svg` Artifact preview, composer Insert menu (`/` prompt files, `#` project files), drag-and-drop attachments, Explain-this-Customization. This wave added the backend + remaining frontend: conversation **search** (`GET /api/conversations/search`, title+message text, snippet), **pin/archive** (conversation flags persisted via Save/Import store; `patchConversation` accepts them without bumping updatedUtc; `listConversations` sorts pinned-first; per-item ⋯ menu), Markdown **export** (client-side transcript download), durable **Preferences** (`Get-DpDefaultSettings`/`Merge-DpSettings` validated 8000-char; injected as an About-the-user block in `New-DpTurnParameter`), Git **undo + diff** (`Get-DpGitDiff` working-tree-vs-HEAD + untracked→content; `Invoke-DpGitRestore` revert-to-HEAD + delete-untracked, path-confined; inline coloured diff + ↩ Undo in the Activity panel), **Atelier health** (`Get-DpAtelierHealth` per-root resolve/count/junction; Settings panel), and the `#file` backend (`Get-DpFileFind`, project-confined recursive search). 5 new routes registered (search before `/{id}`). Specs 010/060 + glossary updated (FR-C10–C14, FR-T6/T7, FR-M7/M8, FR-S4, FR-X6, FR-G1; Preferences + Artifact terms). Verified: 175/175 Pester (+22, incl. guarded real-repo Git tests); 11/11 live HTTP endpoint smoke (atelier, preferences round-trip, fs/find, git/diff isRepo, git/restore 400, search-by-title, pin, archive, list flags, no-token 401); `node --check` clean; every `.ps1` parses. |
| 2026-06-11 | Shipped a second QoL batch (Phase 2.6): **regenerate** the last response and **edit & resend** a user Message (new `Reset-DpConversationForRerun` truncates to a user Message + rebuilds Engine history; routes `POST /api/conversations/{id}/regenerate` and `/edit`, registered before `/stop`; the `start` SSE frame now carries `userMessageId`); a **command palette** (Ctrl/Cmd+K) + shortcuts (Ctrl/Cmd+Shift+O, `/`); **reference files** (`referenceFiles` Setting, paths injected into the Turn system prompt as build-free light retrieval); and a per-session **spend warning** (`costBudgetUSD` Setting + one-time toast). Frontend: per-message ↻ regenerate (last only) + ✎ edit-in-place, shared `_streamRerun`, palette modal, reference-files + budget Settings fields. Specs 010/060 + glossary updated (FR-C15/C16, FR-M9/M10/M11; Reference File term; roadmap Phase 2.6). Verified: 186/186 Pester (+11); 7/7 round-2 endpoint smoke (regenerate 400/404, edit 400, refs dedup, budget set + negative-reject); `node --check` clean; PSScriptAnalyzer baseline-consistent (fixed a `$matches` automatic-variable shadow + an em-dash for the no-BOM convention). |
| 2026-06-11 | **Hotfix:** the Phase 2.6 commit shipped a scrambled `app.js` (a multi-edit transposed the `explainCustomization` prompt template and the Command-palette / `function wireGlobal` header lines, deleting `function wireGlobal`). The browser loads `app.js` as an ES module, so the syntax error aborted the whole bundle — `init()` never ran, leaving the UI hung with no models and a disabled Send. `node --check` had falsely passed it in script mode. Repaired the three scrambled regions; verified by ESM-checking the **served** bytes (`node --check` on a `.mjs` copy = browser parser) + 39 models load + Pester 186/0. Recorded the ESM-validation rule in `.memory-bank/debugging-insights.md`. |

## In progress

- (none — bootstrap complete; next work is Phase 1.)

## Backlog (near-term, Phase 1)

- Stop-a-Turn wired to the UI; live Activity events during a Turn.
- Ask-User Tool routed into the thread (FR-T4).
- Disk persistence of Conversations (FR-C7).
- Confirm-before-run gate for Terminal and out-of-folder writes.
- Optional: manual browser smoke of a streaming Turn (the path is already
  verified headlessly by `tests/live`).

## Backlog (later)

- Vision (image attach) and structured-output surfaces.
- User Tool management UI (`Register-ShpTool`).
- WebView2 single-window desktop shell; packaging/installer. Revisit as a
  separate, decoupled track; weigh a **cross-platform** shell (Tauri/Photino/
  Electron/MAUI BlazorWebView) against Windows-only WebView2 because
  cross-platform is a first-class value. Motivation: single-window app feel,
  own taskbar/Alt-Tab entry, hide the localhost URL from non-technical users.
- Multi-client concurrency for the Host Server.
- **Consider running the live tests in CI** (behind a stored Copilot token
  secret + accepted per-run credit cost). Current plan is unit-Pester-only in
  CI. Parked from the 2026-06-12 publish/Sampler grill-me interview.

## Decision log

- **2026-06-08 — PowerShell Host Server over Electron/Node.** In-process Engine
  integration, zero extra runtime, fit with the PowerShell ecosystem.
- **2026-06-08 — `TcpListener` + hand-rolled HTTP over `HttpListener`.** Avoids
  Windows URL-ACL/admin friction; fully cross-platform on loopback.
- **2026-06-08 — SSE over WebSockets.** One-directional token streaming is
  simpler and more robust.
- **2026-06-08 — Conversation isolation via `-History`.** Avoids the Engine's
  module-scoped running chat bleeding across Conversations.
- **2026-06-08 — Build-free static SPA.** No toolchain for end users.
- **2026-06-08 — In-house XSS-safe Markdown renderer.** Escape-first, safe
  subset; no third-party script to vendor or audit.
- **2026-06-12 — Publish to the Gallery + convert to Sampler (signed off).**
  Grill-me interview verdict: convert DeskPilot to a **Lean Sampler** project
  now (ModuleBuilder + InvokeBuild + GitVersion + Pester 5 + PSScriptAnalyzer;
  no DSC/Datum); make it **publish-ready now, publish-for-real later** (deferred
  until a stable ShellPilot release or a first user). Dependency model: declare
  ShellPilot in `RequiredModules` with a pinned minimum tested version (needs a
  stable, non-prerelease ShellPilot first; `Resolve-DpEngineModule` demoted to a
  fallback). Packaging fixes found in the interview: give `Start-DeskPilot` a
  module-relative `-WebRoot` default (it is currently `[Parameter(Mandatory)]`),
  ModuleBuilder `CopyPaths` to bundle `web/`, and remove the hardcoded `0.1.0`
  (GitVersion owns the version, in both the manifest and the
  `$script:DeskPilot.Version` hashtable). CI mirrors ShellPilot, unit-Pester
  only, a built-module smoke gate (import, resolve web root, `/api/health`)
  before publish, protected `main` (PR-only) + green CI + manifest-validate,
  auto-publish on merge to main but gated OFF until go-live. Ship unsigned with a
  cross-platform `iwr|iex` bootstrap (in-repo, HTTPS, tag-pinned; forces
  CurrentUser, trusts PSGallery, sets TLS 1.2, verifies PS7). No telemetry
  (Gallery download counts only); CI/version/downloads badges; fail-silent
  launch-time version + update check. Fix manifest metadata
  (ProjectUri→raandree/DeskPilot, real Author, IconUri, fuller description); add
  a `LICENSE` (MIT) file. WebView2/desktop shell parked as a decoupled,
  cross-platform-aware later track.
