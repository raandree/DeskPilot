Create a file named `eval-selfcheck.json` in the root of this workspace.

It must be valid JSON with exactly these three fields:

- `case` — the string `structured-run-report`
- `status` — the string `ok`
- `filesChanged` — the number `1`

Create no other file, change no existing file, and do not commit.
