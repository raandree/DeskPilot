In `source/Private/Invoke-DpTurn.ps1`, add a three-line comment immediately above
the line that reads `$sealNarration = {`, explaining in your own words what that
scriptblock does.

Change absolutely nothing else in the file, and do not commit anything.
