Some tests in this repository are reported as failing. Establish the authoritative
result and report it exactly.

Run the repository's own gate — `./build.ps1 -Tasks test` from the repository root —
wait for it to finish, and report the exact number of tests passed, failed and
skipped that it reports.

Do not change any file. Do not commit anything.
